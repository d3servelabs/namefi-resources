import type { ConfigInput } from '../schema';

// Development uses the dev nameservers (ns3/ns4.namefi.dev.) via schema defaults.
const developmentConfig: ConfigInput = {};

export default developmentConfig;
