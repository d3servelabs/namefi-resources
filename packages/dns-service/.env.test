# Test env for @namefi-astra/dns-service. Dummy values only — the DNS unit
# tests are pure or mock #lib/env/#lib/logger, so no live DB/Redis is touched
# (the Postgres pool is constructed lazily and never queried here).
ENVIRONMENT=test
DATABASE_URL=postgresql://test:test@localhost:5432/test
DATABASE_DRIVER=pg
