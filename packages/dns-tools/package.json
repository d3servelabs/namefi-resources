{
  "name": "@namefi-astra/dns-tools",
  "author": "Namefi Open Source <oss@namefi.io>",
  "license": "MIT",
  "description": "DNS tools created and maintained by Namefi Team",
  "version": "0.0.0-pre.1",
  "type": "module",
  "scripts": {
    "clean": "git clean -xdf .next .turbo node_modules out dist",
    "typecheck": "tsc --noEmit",
    "test": "vitest --run",
    "test:coverage": "bun vitest run --coverage",
    "build": "tsup",
    "dns-tools": "node dist/bin.js"
  },
  "bin": {
    "dns-tools": "./dist/bin.js"
  },
  "dependencies": {
    "commander": "^14.0.0"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "peerDependencies": {
    "vitest": "^3.1.2",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@namefi-astra/typescript": "workspace:*",
    "tsup": "^8.4.0",
    "vite-tsconfig-paths": "^5.1.4"
  }
}
