{
  "name": "@namefi-astra/client",
  "author": "Namefi <dev-team@namefi.io>",
  "license": "MIT",
  "description": "API client for Namefi",
  "version": "0.0.0-pre.13",
  "type": "module",
  "scripts": {
    "prebuild": "./switch-config-copy.sh build",
    "prepublish": "./switch-config-copy.sh publish",
    "clean": "git clean -xdf .next .turbo node_modules out dist",
    "typecheck": "tsgo --noEmit",
    "build": "tsup",
    "generate:contract": "bun ./scripts/minify-contract.ts",
    "check": "biome check .",
    "check:error": "biome check . --diagnostic-level=error",
    "check:fix": "biome check --write .",
    "check:unsafe": "biome check --write --unsafe ."
  },
  "dependencies": {
    "@namefi-astra/backend": "workspace:*",
    "@orpc/client": "^1.14.6",
    "@orpc/contract": "^1.14.6",
    "@orpc/openapi-client": "^1.14.6",
    "@types/node": "^22.13.14"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "peerDependencies": {
    "viem": "catalog:",
    "vitest": "^3.2.6",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@namefi-astra/typescript": "workspace:*",
    "tsup": "^8.4.0",
    "vite-tsconfig-paths": "^5.1.4"
  }
}
