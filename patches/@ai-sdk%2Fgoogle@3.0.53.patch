diff --git a/dist/index.d.ts b/dist/index.d.ts
index b36bf96d..1d0a65d1 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -213,8 +213,12 @@ type GoogleVideoModelOptions = {
     personGeneration?: 'dont_allow' | 'allow_adult' | 'allow_all' | null;
     negativePrompt?: string | null;
     referenceImages?: Array<{
-        bytesBase64Encoded?: string;
-        gcsUri?: string;
+        image: {
+            bytesBase64Encoded?: string;
+            gcsUri?: string;
+            mimeType?: string;
+        };
+        referenceType?: 'asset' | 'style';
     }> | null;
     [key: string]: unknown;
 };
diff --git a/dist/index.js b/dist/index.js
index 3e7f2396..811566ad 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -2248,28 +2248,34 @@ var GoogleGenerativeAIVideoModel = class {
       } else {
         const base64Data = typeof options.image.data === "string" ? options.image.data : (0, import_provider_utils15.convertUint8ArrayToBase64)(options.image.data);
         instance.image = {
-          inlineData: {
-            mimeType: options.image.mediaType || "image/png",
-            data: base64Data
-          }
+          bytesBase64Encoded: base64Data,
+          mimeType: options.image.mediaType || "image/png"
         };
       }
     }
     if ((googleOptions == null ? void 0 : googleOptions.referenceImages) != null) {
       instance.referenceImages = googleOptions.referenceImages.map((refImg) => {
-        if (refImg.bytesBase64Encoded) {
+        if (refImg.image.bytesBase64Encoded) {
           return {
-            inlineData: {
-              mimeType: "image/png",
-              data: refImg.bytesBase64Encoded
-            }
+            image: {
+              bytesBase64Encoded: refImg.image.bytesBase64Encoded,
+              mimeType: refImg.image.mimeType || "image/png"
+            },
+            referenceType: refImg.referenceType || "asset"
           };
-        } else if (refImg.gcsUri) {
+        } else if (refImg.image.gcsUri) {
           return {
-            gcsUri: refImg.gcsUri
+            image: {
+              gcsUri: refImg.image.gcsUri,
+              mimeType: refImg.image.mimeType || "image/png"
+            },
+            referenceType: refImg.referenceType || "asset"
           };
         }
-        return refImg;
+        return {
+          ...refImg,
+          referenceType: refImg.referenceType || "asset"
+        };
       });
     }
     const parameters = {
@@ -2452,8 +2458,12 @@ var googleVideoModelOptionsSchema = (0, import_provider_utils15.lazySchema)(
       negativePrompt: import_v414.z.string().nullish(),
       referenceImages: import_v414.z.array(
         import_v414.z.object({
-          bytesBase64Encoded: import_v414.z.string().nullish(),
-          gcsUri: import_v414.z.string().nullish()
+          image: import_v414.z.object({
+            bytesBase64Encoded: import_v414.z.string().nullish(),
+            gcsUri: import_v414.z.string().nullish(),
+            mimeType: import_v414.z.string().nullish()
+          }),
+          referenceType: import_v414.z.enum(["asset", "style"]).nullish()
         })
       ).nullish()
     }).passthrough()
@@ -2550,4 +2560,4 @@ var google = createGoogleGenerativeAI();
   createGoogleGenerativeAI,
   google
 });
-//# sourceMappingURL=index.js.map
\ No newline at end of file
+//# sourceMappingURL=index.js.map
diff --git a/dist/index.mjs b/dist/index.mjs
index e5503d04..5ed8d180 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -2299,28 +2299,34 @@ var GoogleGenerativeAIVideoModel = class {
       } else {
         const base64Data = typeof options.image.data === "string" ? options.image.data : convertUint8ArrayToBase64(options.image.data);
         instance.image = {
-          inlineData: {
-            mimeType: options.image.mediaType || "image/png",
-            data: base64Data
-          }
+          bytesBase64Encoded: base64Data,
+          mimeType: options.image.mediaType || "image/png"
         };
       }
     }
     if ((googleOptions == null ? void 0 : googleOptions.referenceImages) != null) {
       instance.referenceImages = googleOptions.referenceImages.map((refImg) => {
-        if (refImg.bytesBase64Encoded) {
+        if (refImg.image.bytesBase64Encoded) {
           return {
-            inlineData: {
-              mimeType: "image/png",
-              data: refImg.bytesBase64Encoded
-            }
+            image: {
+              bytesBase64Encoded: refImg.image.bytesBase64Encoded,
+              mimeType: refImg.image.mimeType || "image/png"
+            },
+            referenceType: refImg.referenceType || "asset"
           };
-        } else if (refImg.gcsUri) {
+        } else if (refImg.image.gcsUri) {
           return {
-            gcsUri: refImg.gcsUri
+            image: {
+              gcsUri: refImg.image.gcsUri,
+              mimeType: refImg.image.mimeType || "image/png"
+            },
+            referenceType: refImg.referenceType || "asset"
           };
         }
-        return refImg;
+        return {
+          ...refImg,
+          referenceType: refImg.referenceType || "asset"
+        };
       });
     }
     const parameters = {
@@ -2503,8 +2509,12 @@ var googleVideoModelOptionsSchema = lazySchema12(
       negativePrompt: z14.string().nullish(),
       referenceImages: z14.array(
         z14.object({
-          bytesBase64Encoded: z14.string().nullish(),
-          gcsUri: z14.string().nullish()
+          image: z14.object({
+            bytesBase64Encoded: z14.string().nullish(),
+            gcsUri: z14.string().nullish(),
+            mimeType: z14.string().nullish()
+          }),
+          referenceType: z14.enum(["asset", "style"]).nullish()
         })
       ).nullish()
     }).passthrough()
@@ -2600,4 +2610,4 @@ export {
   createGoogleGenerativeAI,
   google
 };
-//# sourceMappingURL=index.mjs.map
\ No newline at end of file
+//# sourceMappingURL=index.mjs.map
diff --git a/src/google-generative-ai-video-model.ts b/src/google-generative-ai-video-model.ts
index b31633c9..8f62bde3 100644
--- a/src/google-generative-ai-video-model.ts
+++ b/src/google-generative-ai-video-model.ts
@@ -32,8 +32,12 @@ export type GoogleVideoModelOptions = {
 
   // Reference images (for style/asset reference)
   referenceImages?: Array<{
-    bytesBase64Encoded?: string;
-    gcsUri?: string;
+    image: {
+      bytesBase64Encoded?: string;
+      gcsUri?: string;
+      mimeType?: string;
+    };
+    referenceType?: 'asset' | 'style';
   }> | null;
 
   [key: string]: unknown; // For passthrough
@@ -102,29 +106,35 @@ export class GoogleGenerativeAIVideoModel implements Experimental_VideoModelV3 {
             : convertUint8ArrayToBase64(options.image.data);
 
         instance.image = {
-          inlineData: {
-            mimeType: options.image.mediaType || 'image/png',
-            data: base64Data,
-          },
+          bytesBase64Encoded: base64Data,
+          mimeType: options.image.mediaType || 'image/png',
         };
       }
     }
 
     if (googleOptions?.referenceImages != null) {
       instance.referenceImages = googleOptions.referenceImages.map(refImg => {
-        if (refImg.bytesBase64Encoded) {
+        if (refImg.image.bytesBase64Encoded) {
           return {
-            inlineData: {
-              mimeType: 'image/png',
-              data: refImg.bytesBase64Encoded,
+            image: {
+              bytesBase64Encoded: refImg.image.bytesBase64Encoded,
+              mimeType: refImg.image.mimeType || 'image/png',
             },
+            referenceType: refImg.referenceType || 'asset',
           };
-        } else if (refImg.gcsUri) {
+        } else if (refImg.image.gcsUri) {
           return {
-            gcsUri: refImg.gcsUri,
+            image: {
+              gcsUri: refImg.image.gcsUri,
+              mimeType: refImg.image.mimeType || 'image/png',
+            },
+            referenceType: refImg.referenceType || 'asset',
           };
         }
-        return refImg;
+        return {
+          ...refImg,
+          referenceType: refImg.referenceType || 'asset',
+        };
       });
     }
 
@@ -363,8 +373,12 @@ const googleVideoModelOptionsSchema = lazySchema(() =>
         referenceImages: z
           .array(
             z.object({
-              bytesBase64Encoded: z.string().nullish(),
-              gcsUri: z.string().nullish(),
+              image: z.object({
+                bytesBase64Encoded: z.string().nullish(),
+                gcsUri: z.string().nullish(),
+                mimeType: z.string().nullish(),
+              }),
+              referenceType: z.enum(['asset', 'style']).nullish(),
             }),
           )
           .nullish(),
