# Lib to share code

This lib is used to share code
- when code is used in both tRPC and temporal
- when multiple tRPC routers are using the same code
- when multiple temporal workers are using the same code

