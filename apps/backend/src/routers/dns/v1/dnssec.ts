/**
 * @deprecated Moved to `@namefi-astra/dns-service`. This re-export shim keeps
 * the backend's `app.route('v1/dnssec', dnssecRouter)` mount working and will
 * be dropped later.
 */
export * from '@namefi-astra/dns-service/routers/dns/v1/dnssec';
